import React from 'react';

import './styles/NotFound.scss';

const NotFound: React.FC = () => (
  <div className="notification notification_not-found is-danger has-text-centered is-centered-h">
    NOT FOUND
  </div>
);

export default NotFound;
